const numeros = [100, 200, 300, 400, 500, 600, 700, 800, 900, 1000];

// A primeira expressaõ: é executada apenas uma vez.
// A segunda expressaõ: condição de execução.
// A terceira expressão: é executada sempre ao final do bloco.


for (let indice = 0; indice < numeros.length; indice++) {
    console.log(numeros[indice])
}